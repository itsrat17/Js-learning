// const accountID = 1345
// let accountBalance = 1000.50
// var accountStatus = 'active'
// accountMinBalance = 250.75
// let accountType; // Declared but not initialized

// // accountID = 2000 // This will throw an error because accountID is a constant
// accountBalance += 500 // This is valid, accountBalance is declared with let
// accountStatus = 'inactive' // This is valid, accountStatus is declared with var (var is not recommended for use in modern JavaScript, why? because of its function scope)
// accountMinBalance = 300.00 // This is valid, accountMinBalance is implicitly global

// console.table([accountBalance, accountStatus, accountMinBalance, accountType])

// // Recommended: Always declare variables with let or const to avoid unintended global variables
// // Avoid using var in modern JavaScript due to its function scope behavior
// // Always initialize variables to avoid undefined values
// // Use const for variables that should not be reassigned



